#include<stdio.h>
int main()
{
    int number;
    printf("enter an integer");
    scanf("%d",&number);
  if    (number%2==0)
    printf("the given number is an even number");
      else
    printf("the given number is odd");

 return 0;
}
